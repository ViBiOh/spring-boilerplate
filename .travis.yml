language: java
sudo: required
jdk:
- oraclejdk8
services:
- docker
script:
- docker login -u=${DOCKER_USER} -p=${DOCKER_PASS}
- mvn clean install docker:build
- docker push ${DOCKER_USER}/spring-web-bp
notifications:
  email: false
